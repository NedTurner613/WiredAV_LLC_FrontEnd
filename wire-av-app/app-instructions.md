# App Instructions

## Run the mock API

From the project root, run:

```bash
npx json-server src/app/lib/db.json
```

When you are done, stop it with:

```bash
Ctrl+C
```

## Run the Next.js app

From the project root, run:

```bash
npm run dev
```

This file holds the main development instructions for the app.
